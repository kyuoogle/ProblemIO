<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.problemio.user.mapper.UserMapper">

    <resultMap id="UserResponseMap" type="com.problemio.user.dto.UserResponse">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="nickname" column="nickname"/>
        <result property="profileImageUrl" column="profile_image_url"/>
        <result property="statusMessage" column="status_message"/>
    </resultMap>

    <select id="findById" resultMap="UserResponseMap">
        SELECT id, email, nickname, profile_image_url, status_message
        FROM users
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <update id="updateProfile">
        UPDATE users
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="profileImageUrl != null">profile_image_url = #{profileImageUrl},</if>
            <if test="statusMessage != null">status_message = #{statusMessage},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE users SET password_hash = #{passwordHash}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="deleteUser">
        UPDATE users SET is_deleted = 1, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="countFollowers" resultType="int">
        SELECT COUNT(*) FROM follows WHERE following_id = #{userId}
    </select>

    <select id="countFollowings" resultType="int">
        SELECT COUNT(*) FROM follows WHERE follower_id = #{userId}
    </select>

    <select id="countCreatedQuizzes" resultType="int">
        SELECT COUNT(*) FROM quizzes WHERE user_id = #{userId} AND is_public = 1
    </select>

<!--  닉네임 중복 확인  -->
    <select id="existsByNickname" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE nickname = #{nickname} AND is_deleted = 0
    </select>
</mapper>